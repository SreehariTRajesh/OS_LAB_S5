int main(){
    decl
        int temp,pid1,pid2,pid3;
    enddecl
    begin
        pid1=exposcall("Fork");
        write(pid1);
        pid2=exposcall("Fork");
        write(pid2);
        pid3=exposcall("Fork");
        write(pid3);
        return 0;
    end
}